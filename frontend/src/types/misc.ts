enum AppTheme {
    Dark = 'Dark',
    Light = 'Light',
}

enum Language {
    EN = 'en',
    PT = 'pt',
}

enum WebSocketStatus {
    CONNECTED = "CONNECTED",
    DISCONNECTED = "DISCONNECTED",
    CONNECTING = "CONNECTING",
    ERROR = "ERROR"
}

export { AppTheme, Language, WebSocketStatus };